/**
 * @format
 */

import {AppRegistry} from 'react-native';
import EmojiScreen from './screen/EmojiScreen/EmojiScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => EmojiScreen);
